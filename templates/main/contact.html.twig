{% extends "base.html.twig" %}

{% block body %}


<div class="container">
    <h2 class="game-title text-center mb-5" > Nous contacter</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-primary" type="submit">Envoyer</button>
{{ form_end(form) }}

</div>
{% endblock %}