{% extends 'base.html.twig' %}

{% block title %}Ajouter un jeu{% endblock %}

{% block body %}
<h2 class="text-center mt-5 mb-3 game-title">Ajouter un jeu</h2>
<div class="container">

    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    <button class="btn btn-primary" type="submit">Ajouter</button>
    {{ form_end(formView) }}

</div>

{% endblock %}
